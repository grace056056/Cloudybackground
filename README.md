
  # Design logo with cat

  This is a code bundle for Design logo with cat. The original project is available at https://www.figma.com/design/Nfeo5mYkSh8StA2CcS7p8W/Design-logo-with-cat.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  